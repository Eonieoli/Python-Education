🎯 Day 5 종합 실습: 상품 관리 프로그램 (CLI 버전)
시간: 50분 | 클래스 + 예외 처리 + JSON 완성!

📌 요구사항
- Product 클래스 정의 (name, price, stock)
- 함수로 기능 분리
- 타입 힌트 필수
- 예외 처리 필수

📌 기초 문제 (12분)
1. Product 클래스 구현 (타입 힌트)
   class Product:
       def __init__(self, name: str, price: int, stock: int):
           # 검증 로직 포함
           if price < 0:
               raise ValueError("가격은 0 이상이어야 합니다")
           ...

2. 상품 추가 함수
   def add_product(
       products: List[Product], 
       **product_data
   ) -> None:
       product = Product(**product_data)
       products.append(product)

3. 상품 조회 함수
   def get_products(products: List[Product]) -> List[Dict[str, any]]:
       return [p.to_dict() for p in products]

📌 응용 문제 (18분)
4. 상품 검색 함수 (이름으로)
   def find_product(
       products: List[Product], 
       name: str
   ) -> Optional[Product]:
       ...

5. 상품 수정 함수
6. 상품 삭제 함수

7. 가격 범위 필터링 (람다 사용) ⭐
   filtered = list(filter(
       lambda p: min_price <= p.price <= max_price, 
       products
   ))

8. 예외 처리 추가 ⭐⭐
   - 음수 가격 → ValueError
   - 음수 재고 → ValueError
   - 존재하지 않는 상품 → ValueError


📌 도전 문제 (20분)
9. 재고 관리 (입고/출고) ⭐⭐
   class Product:
       def sell(self, quantity: int) -> None:
           if quantity > self.stock:
               raise ValueError("재고 부족")
           self.stock -= quantity
       
       def restock(self, quantity: int) -> None:
           if quantity < 0:
               raise ValueError("음수 불가")
           self.stock += quantity

10. JSON 저장/불러오기 + 언패킹 ⭐⭐⭐
    def save_products(products: List[Product], filename: str) -> None:
        data = [p.to_dict() for p in products]
        with open(filename, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
    
    def load_products(filename: str) -> List[Product]:
        with open(filename, 'r', encoding='utf-8') as f:
            data = json.load(f)
        return [Product(**item) for item in data]  # 언패킹!

11. 람다로 상품 정렬 (가격순, 이름순, 재고순)
    sorted(products, key=lambda p: p.price)
    sorted(products, key=lambda p: p.name)
    sorted(products, key=lambda p: p.stock)

12. 전체 시스템 통합 ⭐⭐⭐
    - while문으로 메뉴
    - 모든 기능 연결
    - 예외 처리로 안정성 확보