# 1_basic.py

"""
Day 4 - 1교시/2교시: 함수 기본
Basic (강사 시연용)

학습 목표:
- def 키워드로 함수 정의하기
- 함수 호출과 return 이해
- 매개변수와 반환값 사용
- 여러 개 반환 (튜플)
- 함수 없이 vs 함수 있을 때 비교
"""


# ============================================
# 📌 1교시: 복습 + 개념 완전 정복 (PPT)
# 📌 2교시: 코드 실습 시작
# ============================================


# ============================================
# 1. 함수란? - 첫 번째 함수 만들기
# ============================================

# 함수는 특정 작업을 수행하는 코드 묶음입니다
# def 키워드로 함수를 정의합니다

def greet():
    """간단한 인사 함수"""
    print("안녕하세요!")

# 함수 호출 - 함수명()
greet()  # 출력: 안녕하세요!
greet()  # 출력: 안녕하세요!
# 같은 코드를 여러 번 실행할 수 있습니다


# ============================================
# 2. 매개변수 - 함수에 값 전달하기
# ============================================

# 매개변수를 사용하면 함수에 값을 전달할 수 있습니다
def greet_person(name):
    """이름을 받아서 인사하는 함수"""
    print(f"안녕하세요, {name}님!")

greet_person("홍길동")  # 출력: 안녕하세요, 홍길동님!
greet_person("김철수")  # 출력: 안녕하세요, 김철수님!


# 여러 개의 매개변수도 가능합니다
def introduce(name, age):
    """이름과 나이를 받아서 소개하는 함수"""
    print(f"제 이름은 {name}이고, {age}살입니다.")

introduce("이영희", 25)  # 출력: 제 이름은 이영희이고, 25살입니다.
introduce("박민수", 30)  # 출력: 제 이름은 박민수이고, 30살입니다.


# ============================================
# 3. return - 함수에서 값 반환하기
# ============================================

# return을 사용하면 함수가 값을 돌려줍니다
def add(a, b):
    """두 숫자를 더하는 함수"""
    result = a + b
    return result

# 반환값을 변수에 저장할 수 있습니다
total = add(10, 20)
print(f"10 + 20 = {total}")  # 출력: 10 + 20 = 30

# 직접 출력도 가능합니다
print(f"5 + 3 = {add(5, 3)}")  # 출력: 5 + 3 = 8


# return이 없으면 None을 반환합니다
def say_hello():
    print("Hello!")
    # return이 없음

result = say_hello()  # 출력: Hello!
print(f"반환값: {result}")  # 출력: 반환값: None


# ============================================
# 4. 여러 개 반환 - 튜플 활용
# ============================================

# 함수는 여러 개의 값을 반환할 수 있습니다 (튜플로 반환)
def get_person_info():
    """사람 정보를 반환하는 함수"""
    name = "홍길동"
    age = 25
    city = "서울"
    return name, age, city  # 튜플로 반환

# 언패킹으로 여러 변수에 한 번에 할당
person_name, person_age, person_city = get_person_info()
print(f"이름: {person_name}")  # 출력: 이름: 홍길동
print(f"나이: {person_age}")  # 출력: 나이: 25
print(f"도시: {person_city}")  # 출력: 도시: 서울


def calculate_stats(numbers):
    """리스트의 합계와 평균을 반환하는 함수"""
    total = sum(numbers)
    average = total / len(numbers)
    return total, average

scores = [85, 90, 78, 92, 88]
total_score, avg_score = calculate_stats(scores)
print(f"총점: {total_score}")  # 출력: 총점: 433
print(f"평균: {avg_score}")  # 출력: 평균: 86.6


# ============================================
# 5. 함수 없이 vs 함수 있을 때 비교
# ============================================

print("\n=== 함수 없이 작성한 코드 ===")
# 할인가 계산을 3번 반복
price1 = 50000
discount1 = 0.2
final1 = price1 * (1 - discount1)
print(f"상품1 할인가: {final1}원")

price2 = 30000
discount2 = 0.1
final2 = price2 * (1 - discount2)
print(f"상품2 할인가: {final2}원")

price3 = 80000
discount3 = 0.3
final3 = price3 * (1 - discount3)
print(f"상품3 할인가: {final3}원")
# 같은 계산 로직이 반복됩니다!


print("\n=== 함수로 작성한 코드 ===")
# 함수로 만들면 재사용 가능!
def calculate_discounted_price(price, discount):
    """할인가를 계산하는 함수"""
    return price * (1 - discount)

# 같은 함수를 여러 번 호출
print(f"상품1 할인가: {calculate_discounted_price(50000, 0.2)}원")
print(f"상품2 할인가: {calculate_discounted_price(30000, 0.1)}원")
print(f"상품3 할인가: {calculate_discounted_price(80000, 0.3)}원")
# 코드가 훨씬 간결하고 관리하기 쉽습니다!


# ============================================
# 6. 실전 예제: 사용자 등급 판정 시스템
# ============================================

def determine_user_grade(points):
    """포인트에 따라 사용자 등급을 판정하는 함수"""
    if points >= 1000:
        return "VIP"
    elif points >= 500:
        return "Gold"
    elif points >= 100:
        return "Silver"
    else:
        return "Bronze"

# 여러 사용자의 등급 판정
users = [
    ("김철수", 1500),
    ("이영희", 300),
    ("박민수", 800),
    ("최지은", 50)
]

print("\n=== 사용자 등급 판정 ===")
for name, points in users:
    grade = determine_user_grade(points)
    print(f"{name}님 ({points}점): {grade} 등급")

# 출력:
# 김철수님 (1500점): VIP 등급
# 이영희님 (300점): Silver 등급
# 박민수님 (800점): Gold 등급
# 최지은님 (50점): Bronze 등급
